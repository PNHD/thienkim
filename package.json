{
  "name": "tk-pipeline",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute tk-pipeline-db --local --file=src/db/schema.sql",
    "db:init:remote": "wrangler d1 execute tk-pipeline-db --remote --file=src/db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
